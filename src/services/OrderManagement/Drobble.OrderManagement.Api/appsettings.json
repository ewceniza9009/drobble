{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Remember to use 'localhost' since we're running the API outside of Docker!
    "PostgresConnection": "Host=localhost;Port=5432;Database=drobble-db;Username=postgres;Password=devpass"
  }
}